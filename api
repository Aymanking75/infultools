export default async function handler(req: any, res: any) {
  if (req.method !== "POST") {
    return res.status(405).json({ error: "Method not allowed" });
  }

  const { prompt } = req.body;

  // رد مؤقت (Mock)
  res.status(200).json({
    text: "هذه إجابة تجريبية من المساعد الذكي"
  });
}